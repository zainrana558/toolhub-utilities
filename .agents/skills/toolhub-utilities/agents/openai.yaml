interface:
  display_name: "Toolhub Utilities"
  short_description: "Repo-specific patterns and workflows for toolhub-utilities"
  default_prompt: "Use the toolhub-utilities repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true